.git/

# secrets / local config
.env
.env.*
!.env.example
# backend/.env is the exception — deliberately baked into the image (see
# the COPY in Dockerfile) instead of injected via k8s Secret at runtime.
!backend/.env
backend/site_registry.seed.json

# local dev artifacts
**/__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
backend/.venv/
backend/data/
*.db
*.sqlite3
node_modules/
frontend/dist/
admin-frontend/dist/
.vite/

# editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
*.log
